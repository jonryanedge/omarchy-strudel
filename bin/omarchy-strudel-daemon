#!/bin/bash
# omarchy-strudel-daemon — entry point for the systemd service
set -euo pipefail

LIB_DIR="/usr/lib/omarchy-strudel"
NODE_BIN="$(command -v node || true)"

if [[ -z "$NODE_BIN" ]]; then
  echo "error: node is not installed" >&2
  exit 1
fi

exec "$NODE_BIN" "${LIB_DIR}/src/daemon.mjs"
